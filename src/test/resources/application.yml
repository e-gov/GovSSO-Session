management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  main:
    banner-mode: off

govsso:
  base-url: http://localhost:9877/
  session-max-update-interval-minutes: 20
  session-max-duration-hours: 1000000
  hydra:
    admin-url: https://localhost:9877/
  tara:
    issuer-url: https://localhost:9877
    client-id: testclient123
    client-secret: testsecret123

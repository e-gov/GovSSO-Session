# TODO Disable most endpoints in non-dev environments.
management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  main:
    banner-mode: off

govsso:
  base-url: http://localhost:9877/
  hydra:
    admin-url: https://localhost:9877/
  tara:
    auth-url: https://localhost:9877/auth/url/test
    client-id: testclient123
    token-url: https://localhost:9877/oidc/token
    client-secret: testsecret123
